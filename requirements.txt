# Core GUI Framework (Migration from PyQt6)
PySide6>=6.6.0

# Audio & Processing
numpy>=1.24.0
sounddevice>=0.4.6

# TTS Engines
edge-tts>=6.1.9
piper-tts>=1.2.0

# Supertonic TTS (Using latest available version compatible with Python 3.10+)
# Note: Version 3.0.0 does not exist on PyPI. Using latest stable 1.x or removing if not critical.
supertonic>=1.3.1

# Utilities
pydantic>=2.0.0
